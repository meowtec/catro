{
  "name": "catro",
  "version": "1.0.0",
  "description": "HTTP Proxy core",
  "main": "built/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/meowtec/catro"
  },
  "scripts": {
    "typings": "tsd install",
    "lint": "tslint ./src/*.ts* ./src/**/*.ts* ./src/**/**/*.ts* ./src/**/**/*.ts*",
    "build": "rm -rf built && tsc -p . && PRESET_NODE_VERSION='> 4.0' babel built --out-dir built --presets es2015-auto",
    "test": "npm run lint && npm run build && cp -r src/__tests/resources ./built/__tests/ && mocha built/__tests"
  },
  "author": "Berton Zhu<bertonzh@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-preset-es2015-auto": "^1.0.1",
    "express": "^4.13.4",
    "mocha": "^2.4.5",
    "request": "^2.69.0",
    "tslint": "^3.3.0",
    "typescript": "^1.8.0",
    "typings": "^0.7.9"
  }
}
